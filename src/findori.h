#ifndef FINDORI_H
#define FINDORI_H

#include <string>
#include <vector>

std::vector<int> find_min(std::vector<int> my_skew);

std::vector<int> find_max(std::vector<int> my_skew);

#endif /* FINDORI_H */
